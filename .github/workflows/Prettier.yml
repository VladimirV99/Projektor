name: PrettierCheck

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]
    
jobs:
  CheckPrettier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a one-line script
        run: npx prettier --check .
