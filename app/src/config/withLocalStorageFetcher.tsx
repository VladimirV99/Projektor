import { Fragment } from "react"

export default function WithLocalStorageFetcher({ children }: { children: JSX.Element | JSX.Element[] }) {

    return <Fragment>
        {children}
    </Fragment>

}
