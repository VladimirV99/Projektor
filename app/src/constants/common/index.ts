export enum FetchType {
    InitialLoad,
    LoadingMore,
    Refreshing,
}

export enum LoadingStatus {
    NotInitialized,
    Initializing,
    Fetched,
    Failed,
    FetchingMore,
    Refreshing,
}